const { defaultHashOptions, FFFingerPrinter, defaultHashModeBytes, defaultStreamOptions } = require('./lib/FFFingerPrinter');

module.exports = Object.freeze({
  defaultHashOptions, defaultHashModeBytes, defaultStreamOptions, FFFingerPrinter
});
